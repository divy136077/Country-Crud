import { GetStatusValuePipe } from './get-status-value.pipe';

describe('GetStatusValuePipe', () => {
  it('create an instance', () => {
    const pipe = new GetStatusValuePipe();
    expect(pipe).toBeTruthy();
  });
});
